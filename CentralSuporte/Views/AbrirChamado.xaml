<Page
    x:Class="CentralSuporte.Views.AbrirChamado"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800"
    Title="AbrirChamado">

    <Grid Background="{DynamicResource UiBackground}">
        <ui:Card Margin="40" Padding="30"  Background="{DynamicResource UiControlBackground}">
            <StackPanel>

                <TextBlock Text="Abrir Chamado" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource UiForeground}" Margin="0 0 0 10"/>

                <StackPanel>
                    <TextBlock Text="Título:" FontWeight="Bold"/>
                    <ui:TextBox Text="{Binding Titulo, UpdateSourceTrigger=PropertyChanged}"  />
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Descrição:" FontWeight="Bold"/>
                    <ui:TextBox Text="{Binding Descricao, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Cargo:" FontWeight="Bold"/>
                    <ui:TextBox Text="{Binding Cargo, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <ui:Button Content="Abrir Chamado"
                           Command="{Binding AbrirChamadoCommand}"
                           Appearance="Primary"
                           HorizontalAlignment="Right"
                           Margin="0 20 0 0"
                           Width="150"/>
            </StackPanel>
        </ui:Card>

        <!--<ui:SnackbarPresenter x:Name="SnackbarArea"
                         VerticalAlignment="Bottom"
                         HorizontalAlignment="Right"
                         Margin="0 0 20 20" />-->

        <!--<ContentPresenter x:Name="RootContentDialog"/>-->

    </Grid>
</Page>
